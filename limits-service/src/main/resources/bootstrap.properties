# this application.name is required as the properties will be picked from the config server by this name
spring.application.name=limits-service
server.port=8080
#uri to connect this properties to the config server
spring.cloud.config.uri=http://localhost:8888
spring.profiles.active=dev
#configuration move to central config server
#limits-service.minimum = 99
#limits-service.maximum = 9999
