# this app.name is required as the properties will be picked from the config server by this name
spring.application.name=limits-service
server.port=8080

#uri to connect this property to the config server (previously used)
#spring.cloud.config.uri=http://localhost:8888
#Now used to connect this service with the config server:
spring.config.import= optional:configserver:http://localhost:8888


#after activating dev also it will take not take property from limits-service-dev.properties it's a bug
spring.profiles.active=dev
#Fix is here
spring.cloud.config.profile=dev



##configuration move to central config server
#limits-service.minimum = 99
#limits-service.maximum = 9999
